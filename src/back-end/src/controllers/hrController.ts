const HRManagement = require("../models/HRManagement");

const hrController = {
    // generateToken: async (req, res) => {

    // },

    // viewEmployeeProfiles: async (req, res) => {

    // },

};

module.exports = hrController;
